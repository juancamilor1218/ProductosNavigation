<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <!-- color de fondo del shape -->
            <solid android:color="@color/colorSale"/>
            <!-- tamaño y color del shape -->
            <stroke
                android:color="@color/colorSale"
                android:width="1dp"/>
            <!-- tamaño de las esquinas redondeadas -->
            <corners android:radius="20dp"/>
        </shape>
    </item>
</selector>